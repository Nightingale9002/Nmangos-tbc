# 服务器待办与发现记录（2026-08）

> 本文件记录会话中产生/确认的待办与已确认发现。代码改动未提交，由用户自行 commit。

## 已确认：云端号角任务（5321）
- 云端数据库与本地一致：Kerlonian's Chest (176634) loot 表为空（开不出号角）——**但用户确认云端无问题，不用改**（可能玩家不需要该任务）
- 本地已修复：INSERT gameobject_loot_template (176634, 13536, -100, 1, 1)

## 已确认：9417/9418 掉地下根因（.gps 实测）
- 9418 Mosshide Mongrel (1008) 位置 (-4150.90, -2851.43)，GroundZ=20.41（山体），spawn z=12.12（山脚，正确）
- 问题：随机漫步选点可能落在山体/错误层 → 走进山体/掉地下
- **已修复（代码）**：随机漫步目标点必须贴地（PathFinder::ComputePathToRandomPoint，floorZ 检查：|floorZ - endPoint.z| > 1.0 阻止），待部署云端

## 已确认：每 IP 连接限制（2026-08-22 云端已部署）
- 云端 iptables 已加规则（持久化 systemd 服务 iptables-restore）：
  - hashlimit：8086 端口每 IP 每秒最多 1 个新连接（burst 1，SYN 洪水丢弃）
  - connlimit：8086 端口每 IP 最多 3 个并发连接（约3账号，第 4 个 REJECT）
  - 规则文件 /etc/iptables.rules，重启自动恢复
- 用户实测被踢下线验证生效

## 已确认：任务 484 Young Crocolisk Skins（小鳄鱼皮）
- 接取条件：等级 18，湿地（ZoneOrSort=11），NPC 2094 接取/交还，无前置、无种族/职业限制——**正常，无需修改**

## 待办清单
1. 随机漫步防掉地下（已实现：floorZ 检查，待部署云端）
2. 小鳄鱼皮 484 任务接取条件（已查完，正常）
3. 每个 IP 带宽限制（已完成：iptables 连接限制部署云端）
4. **普通攻击伤害数字延迟**：客户端伤害数字显示比实际计算晚——考虑是否延迟伤害计算；253 客户端已测，需继续测 243
5. **缠绕时攻击目标**：怪物被缠绕（root）等状态时应攻击近战范围内的目标，而不是锁定原远处目标

## 备注
- 云服务器二进制 Aug 20（非最新，水移动修复 + 随机漫步修复未上云，Linux 版需在云端编译：/opt/mangos 无源码目录，编译环境 gcc/cmake 可用）
- 云日志曾持续刷 [LEASH] guid=9418（与随机漫步掉地下相关，修复后应消失）
