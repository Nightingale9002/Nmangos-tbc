-- ============================================================================
-- 076_补哈兰(Halaa)NPC刷点.sql —— 【已作废，纯注释留档，不含任何可执行语句】
-- ============================================================================
--
-- 状态（2026-09-18）：**作废；它当初插入的数据已被 `dev/082` 清除**。
--   · 本文件曾于 2026-09-17 应用过一次，在 `creature` 表插入 40 行常驻刷点
--     （guid 9000001-9000040：10 商人 + 15 组双方卫兵）；
--   · 这 40 行**坐标是估值**（绕哈兰旗帜画的两圈），并非原版位置；
--   · 2026-09-18 站长明确要求"回归原版事件逻辑，而不是我们写的这个"，
--     NPC 改由原版 DB 事件脚本生成 → 这 40 行已由 `dev/082` 删除
--     （云端 marker 已到 082，实测"残留刷点 = 0 / 镇内自制 NPC = 0"）。
--
-- ⚠️ 不要在新库上执行本文件，也不要把下面的语句取消注释 —— 它会把那 40 个错位刷点重新插回去。
--    原内容可在 git 历史里取回：`git show a507553c5:dev/076_补哈兰NPC刷点.sql`。
--
-- 原版正确行为（本文件的替代方案）：
--   · 哈兰初始**中立**（`OutdoorPvPNA` 的 `m_zoneOwner = TEAM_NONE`），镇里没有常驻 NPC；
--   · 某方占领时，代码把事件交回 DB 脚本，由 **`dbscripts_on_event` `11503`（部落）/ `11504`（联盟）**
--     在**原版真实坐标**上召唤该阵营的 **5 商人 + 15 卫兵**（各 20 行，一直存在于我们库里、未被改动）；
--   · 卫兵死亡后按原版逻辑 5 分钟（围城 1 小时）在原地召唤补充；
--   · "玩家走远→网格卸载→NPC 消失"由代码侧补召唤解决
--     （`OutdoorPvPNA::RespawnFactionNpcs`：仅当记下的 NPC 一个都不在图里时才重跑原版脚本）。
--
-- 原 SQL 摘要（仅记录，勿执行）：
--   DELETE FROM creature WHERE guid BETWEEN 9000001 AND 9000040;
--   INSERT INTO creature (guid,id,map,spawnMask,position_x,position_y,position_z,orientation,
--                         spawntimesecsmin,spawntimesecsmax,spawndist,MovementType) VALUES
--     (9000001..9000010, 商人 18816/18817/18821/18822/21474/21483/21484/21485/21487/21488,
--      530, 1, x 约 -1566..-1579 / y 约 7938..7952 / z -22.5, 300, 300, 0, 0);
--     (9000011..9000040, 联盟卫兵 18256 与部落卫兵 18192 成对同坐标, 530, 1,
--      同一批估值坐标, 300, 300, 0, 0);
--
-- 完整来龙去脉见 `dev/KNOWN_ISSUES.md`：
--   ·「哈兰(Halaa)：商人/卫兵刷点本来就没有 + 易主后"走开再回来就没 NPC"」（已标整条回退）
--   ·「哈兰回归原版事件逻辑」+「追加：修掉"走开再回来 NPC 消失"」
-- ============================================================================
