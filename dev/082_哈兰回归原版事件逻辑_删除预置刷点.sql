-- ============================================================================
-- 082_哈兰回归原版事件逻辑_删除预置刷点.sql —— 【已执行完毕，纯注释留档，不含任何可执行语句】
-- ============================================================================
--
-- 状态（2026-09-18 03:02 已在云端执行完毕，marker = **082**）：
--   · 执行结果：`creature` 表里 guid 9000001-9000040（`dev/076` 插的 40 行）全部删除，
--     实测「残留刷点 = 0」「镇内哈兰常驻 NPC = 0」；
--   · 原版事件脚本 `dbscripts_on_event` 11503（部落）/ 11504（联盟）各 20 行**未受影响、仍在库里**。
--   · 至此哈兰完全回归原版：初始中立、镇里无常驻 NPC、占领时由那两条原版脚本在真实坐标召唤。
--
-- ⚠️ 保留为纯注释的原因：功能已经用完（云端/本地库的刷点都已清 0），
--    而且它依赖的 `dev/076` 也已改成纯注释 → 不需要（也不应该）再重复执行。
--    若日后真有人误把 076 的语句恢复执行，把下面的 DELETE 段临时取消注释再跑一次即可（幂等）。
--
-- 背景（站长反馈）："重启后哈兰直接归部落了" + "NPC 刷新位置不对" + "哈兰应该按原来的事件逻辑来"。
--
-- 根因（两条，都是我们自己造成的）：
--   ① 代码侧：fork 的三个 commit（d540af8b7 / 136f51d10 / a507553c5）把哈兰改成自制逻辑
--      "初始归属=部落（`m_zoneOwner(HORDE)`）+ 双方 NPC 常驻 DB 刷点 + 易主时 despawn 输方"
--      → **每次重启哈兰都回到部落手里**。现已整条回退到上游原版（`m_zoneOwner = TEAM_NONE`）。
--   ② 数据侧：`dev/076` 按"初版估值坐标"插了 40 行常驻刷点，**并非原版位置**（全挤在 x -1590..-1556 一小圈；
--      而原版脚本的 x 跨 -1654..-1480，铺满整个镇子）。
--
-- 本文件当时做的事（仅记录，勿执行）：
--   USE tbcmangos;
--   DELETE FROM creature                  WHERE guid BETWEEN 9000001 AND 9000040;
--   DELETE FROM creature_addon            WHERE guid BETWEEN 9000001 AND 9000040;
--   DELETE FROM creature_movement         WHERE id   BETWEEN 9000001 AND 9000040;
--   DELETE FROM creature_conditional_spawn WHERE guid BETWEEN 9000001 AND 9000040;
--   DELETE FROM spawn_group_spawn         WHERE guid BETWEEN 9000001 AND 9000040;
--   DELETE FROM game_event_creature       WHERE guid BETWEEN 9000001 AND 9000040;
--
-- 配套改动（同批上线）：
--   · 代码回退：`src/game/OutdoorPvP/OutdoorPvPNA.cpp/.h` → 与上游原版仅差
--     `d540af8b7` 的 7 行空队列保护（保留，真宕机修复）；
--   · 新增"补召唤"修掉原版毛病"走开再回来 NPC 消失"：`OutdoorPvPNA::RespawnFactionNpcs`
--     （仅当记下的 NPC 一个都不在图里时才重跑原版脚本 11503/11504，幂等；不改归属/坐标/触发条件）。
--
-- 完整来龙去脉见 `dev/KNOWN_ISSUES.md`：
--   ·「哈兰(Halaa)：商人/卫兵刷点本来就没有 + 易主后"走开再回来就没 NPC"」（已标整条回退）
--   ·「哈兰回归原版事件逻辑」+「追加：修掉"走开再回来 NPC 消失"」
-- ============================================================================
